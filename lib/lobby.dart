import 'package:flutter/material.dart';
import 'package:vampulv/user_map/user_map.dart';

class Lobby extends StatelessWidget {
  const Lobby({super.key});

  @override
  Widget build(BuildContext context) {
    return const UserMap();
  }
}
